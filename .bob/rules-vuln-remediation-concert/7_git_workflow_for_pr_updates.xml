<git_workflow_for_pr_updates>
  <overview>
    Critical instructions for properly updating an existing PR with vulnerability remediation fixes
    in the bob-shell automated workflow. The workflow prompt provides the exact branch name to use.
  </overview>

  <critical_principle>
    <title>ALWAYS Use the Branch Name from Workflow Prompt</title>
    <rule>
      The workflow prompt explicitly provides the branch name in step 6:
      "6. Commit your fixes to the EXISTING PR branch: {branch_name}"
      
      You MUST use this exact branch name for all git operations.
    </rule>
    <why_this_matters>
      <reason>The PR is already created and linked to this specific branch</reason>
      <reason>Committing to this branch automatically updates the PR</reason>
      <reason>This triggers the CI/CD workflow to re-run tests</reason>
      <reason>The workflow assigns reviewers based on test results</reason>
      <reason>Using a different branch would not update the PR</reason>
    </why_this_matters>
  </critical_principle>

  <extracting_branch_name>
    <title>Step 1: Extract Branch Name from Workflow Prompt</title>
    
    <workflow_prompt_structure>
      <description>
        Step 6 of the workflow prompt contains the branch name:
        "6. Commit your fixes to the EXISTING PR branch: {branch-name}"
      </description>
    </workflow_prompt_structure>
    
    <parsing_steps>
      <step number="1">
        <action>Locate step 6 in the workflow prompt</action>
        <pattern>6\. Commit your fixes to the EXISTING PR branch: (.+)</pattern>
      </step>
      
      <step number="2">
        <action>Extract the branch name</action>
        <example>
          Input: "6. Commit your fixes to the EXISTING PR branch: fix/cve-2023-12345-flask"
          Extract: fix/cve-2023-12345-flask
        </example>
      </step>
      
      <step number="3">
        <action>Store branch name for git commands</action>
        <usage>
          Use this exact branch name in all subsequent git operations
        </usage>
      </step>
    </parsing_steps>
  </extracting_branch_name>

  <git_workflow_steps>
    <title>Step 2: Execute Git Commands to Update PR</title>

    <step number="1" name="checkout_pr_branch">
      <action>Switch to the PR branch</action>
      <command>git checkout {pr_branch_from_workflow_prompt}</command>
      <example>git checkout fix/cve-2023-12345-flask</example>
      <tool>execute_command</tool>
      <verify>
        <command>git branch --show-current</command>
        <expected_output>fix/cve-2023-12345-flask</expected_output>
      </verify>
      <critical>
        This MUST be the exact branch name from step 6 of the workflow prompt.
        Do NOT create a new branch with git checkout -b.
        Do NOT use a different branch name.
      </critical>
      <if_branch_not_exists>
        <scenario>Local branch doesn't exist yet</scenario>
        <command>git fetch origin {pr_branch_name}</command>
        <then>git checkout {pr_branch_name}</then>
        <example>
          git fetch origin fix/cve-2023-12345-flask
          git checkout fix/cve-2023-12345-flask
        </example>
      </if_branch_not_exists>
    </step>

    <step number="2" name="verify_branch">
      <action>Confirm you're on the correct branch</action>
      <command>git branch --show-current</command>
      <must_match>The branch name from step 6 of the workflow prompt</must_match>
      <if_wrong_branch>
        <error>You're on the wrong branch!</error>
        <fix>Go back to step 1 and checkout the correct branch</fix>
      </if_wrong_branch>
    </step>

    <step number="3" name="stage_changes">
      <action>Stage your fixed files</action>
      <command>git add {file1} {file2} ...</command>
      <example>git add app/vulnerable/main.py app/vulnerable/test_main.py</example>
      <alternative>git add .</alternative>
      <tool>execute_command</tool>
      <verify>
        <command>git status</command>
        <check>Verify the correct files are staged</check>
      </verify>
    </step>

    <step number="4" name="commit_changes">
      <action>Commit with a clear, descriptive message</action>
      <command>git commit -m "{message}"</command>
      <tool>execute_command</tool>
      <message_format>
        fix: resolve unit test failures after {package} upgrade

        - {Specific change 1}
        - {Specific change 2}
        - {Specific change 3}

        Addresses failing tests in PR #{number}
        Related to CVE-{id}
      </message_format>
      <example>
        git commit -m "fix: resolve unit test failures after Flask upgrade

- Fixed import paths for Flask v3.1.0
- Updated test assertions to match new behavior
- Replaced deprecated request.json with request.get_json()

Addresses failing tests in PR #38
Related to CVE-2024-12345"
      </example>
    </step>

    <step number="5" name="push_to_pr_branch">
      <action>Push to the PR branch</action>
      <command>git push origin {pr_branch_from_workflow_prompt}</command>
      <example>git push origin fix/cve-2023-12345-flask</example>
      <tool>execute_command</tool>
      <critical>
        The branch name MUST match the branch from step 6 of the workflow prompt exactly.
        This is the most critical step - pushing to the correct branch updates the PR.
      </critical>
      <what_happens>
        <immediate>
          - Your commit is added to the PR
          - PR is updated with your changes
          - PR shows "1 new commit" or similar
        </immediate>
        <automatic>
          - GitHub triggers the CI/CD workflow
          - .github/workflows/pr-unit-test.yml runs
          - Unit tests are executed
          - Based on results, reviewers are assigned:
            * If tests pass → Software Lead is requested for review
            * If tests fail → SWE is assigned to fix remaining issues
        </automatic>
      </what_happens>
    </step>
  </git_workflow_steps>

  <verification>
    <title>Step 3: Verify PR Was Updated</title>

    <check number="1">
      <what>Push command succeeded</what>
      <how>Check execute_command output</how>
      <expected>No errors, successful push message</expected>
    </check>

    <check number="2">
      <what>Commit is on the correct branch</what>
      <how>Verify branch name matches workflow prompt</how>
      <expected>Branch name from step 6 of workflow prompt</expected>
    </check>
  </verification>

  <common_mistakes>
    <title>Common Mistakes to Avoid</title>

    <mistake name="creating_new_branch">
      <description>Creating a new branch instead of using the workflow-provided branch</description>
      <wrong_command>git checkout -b pr-38</wrong_command>
      <why_wrong>
        This creates a completely new branch that's not connected to the PR.
        Pushing this would require creating a new PR, not updating the existing one.
      </why_wrong>
      <correct_approach>
        Use git checkout {branch_from_workflow_prompt} to switch to the PR's branch
      </correct_approach>
    </mistake>

    <mistake name="wrong_branch_name">
      <description>Using a different branch name than provided in the workflow prompt</description>
      <wrong_example>
        Workflow provides: fix/cve-2023-12345-flask
        You use: fix-tests or main
      </wrong_example>
      <why_wrong>
        The PR is tracking a specific branch. Pushing to a different branch
        won't update the PR.
      </why_wrong>
      <correct_approach>
        Always use the exact branch name from step 6 of the workflow prompt
      </correct_approach>
    </mistake>

    <mistake name="pushing_to_main">
      <description>Accidentally pushing directly to main</description>
      <wrong_command>git push origin main</wrong_command>
      <why_wrong>
        - Bypasses the PR review process
        - Doesn't trigger the PR's CI/CD workflow
        - May be blocked by branch protection rules
        - Doesn't update the PR
      </why_wrong>
      <correct_approach>
        Always push to the PR branch from the workflow prompt, never to main
      </correct_approach>
    </mistake>

    <mistake name="not_fetching_branch">
      <description>Trying to checkout a branch that doesn't exist locally</description>
      <error>error: pathspec 'fix/cve-2023-12345-flask' did not match any file(s)</error>
      <why_happens>
        The PR branch exists on the remote but not in your local repository
      </why_happens>
      <solution>
        <command>git fetch origin {pr_branch_name}</command>
        <then>git checkout {pr_branch_name}</then>
      </solution>
    </mistake>
  </common_mistakes>

  <workflow_integration>
    <title>How This Integrates with the CI/CD Workflow</title>

    <workflow_file>.github/workflows/pr-unit-test.yml</workflow_file>

    <trigger>
      <event>pull_request</event>
      <types>
        <type>opened</type>
        <type>synchronize</type>
        <type>reopened</type>
      </types>
      <explanation>
        The "synchronize" event is triggered when you push new commits to the PR branch.
        This is why pushing to the correct branch is critical.
      </explanation>
    </trigger>

    <workflow_steps>
      <step>Checkout code from the PR branch</step>
      <step>Set up Python environment</step>
      <step>Install dependencies from requirements.txt</step>
      <step>Run unit tests</step>
      <step>Based on results, assign reviewers:
        - All pass → Request review from Software Lead
        - Any fail → Assign to SWE for more fixes
      </step>
    </workflow_steps>

    <why_branch_matters>
      When you push to the PR branch, GitHub:
      1. Detects the push as a "synchronize" event on the PR
      2. Triggers the pr-unit-test.yml workflow
      3. Runs tests on your updated code
      4. Updates the PR with test results
      5. Assigns appropriate reviewers

      If you push to a different branch, none of this happens.
    </why_branch_matters>
  </workflow_integration>

  <quick_reference>
    <title>Quick Reference: Correct Git Workflow</title>

    <checklist>
      <item>✓ Extract branch name from step 6 of workflow prompt</item>
      <item>✓ Checkout that exact branch: git checkout {pr_branch}</item>
      <item>✓ Verify you're on correct branch: git branch --show-current</item>
      <item>✓ Stage your changes: git add {files}</item>
      <item>✓ Commit with clear message: git commit -m "..."</item>
      <item>✓ Push to PR branch: git push origin {pr_branch}</item>
      <item>✓ Verify push succeeded</item>
    </checklist>

    <command_sequence>
      # 1. Extract branch from workflow prompt step 6 (e.g., fix/cve-2023-12345-flask)
      # 2. Checkout the PR branch
      git checkout fix/cve-2023-12345-flask
      
      # 3. Verify you're on the right branch
      git branch --show-current
      
      # 4. Stage your fixes
      git add app/vulnerable/main.py
      
      # 5. Commit with descriptive message
      git commit -m "fix: resolve unit test failures after Flask upgrade

- Fixed import paths for Flask v3.1.0
- Updated test assertions

Addresses failing tests in PR #38"
      
      # 6. Push to the PR branch (NOT a new branch!)
      git push origin fix/cve-2023-12345-flask
    </command_sequence>
  </quick_reference>
</git_workflow_for_pr_updates>

<!-- Made with Bob -->
